matrix:
  include:
    - language: python
      python: 2.7
      before_script:
        - cd src

    - language: node_js
      node_js: "lts/*"
      before_install:
        mkdir ~/.arlobot
        cp scripts/dotarlobot/personalDataForBehavior.json ~/.arlobot/
        cd node
      before_script:
